source -F "#{d:current_file}/themes/everforest_#{@everforest_flavor}_tmux.conf"

%if "#{==:#{@everforest_status_background},default}"
  set -gF @_ef_status_bg "#{@thm_mantle}"
  set -gF status-style "bg=#{@_ef_status_bg},fg=#{@thm_fg}"

  %hidden EF_MESSAGE_BACKGROUND="#{@thm_overlay_0}"
%elif "#{==:#{@everforest_status_background},none}"
  set -g status-style "default"
  set -g @_ef_status_bg "none"

  %hidden EF_MESSAGE_BACKGROUND="default"
%else
  # Treat @everforest_status_background as a format string.
  set -gF status-style "bg=#{E:@everforest_status_background},fg=#{@thm_fg}"
  set -gF @_ef_status_bg "#{E:@everforest_status_background}"

  %hidden EF_MESSAGE_BACKGROUND="#{E:@everforest_status_background}"
%endif

source -F "#{d:current_file}/status/application.conf"
source -F "#{d:current_file}/status/battery.conf"
source -F "#{d:current_file}/status/clima.conf"
source -F "#{d:current_file}/status/cpu.conf"
source -F "#{d:current_file}/status/date_time.conf"
source -F "#{d:current_file}/status/directory.conf"
source -F "#{d:current_file}/status/gitmux.conf"
source -F "#{d:current_file}/status/host.conf"
source -F "#{d:current_file}/status/kube.conf"
source -F "#{d:current_file}/status/load.conf"
source -F "#{d:current_file}/status/pomodoro_plus.conf"
source -F "#{d:current_file}/status/session.conf"
source -F "#{d:current_file}/status/uptime.conf"
source -F "#{d:current_file}/status/user.conf"
source -F "#{d:current_file}/status/weather.conf"

# messages
set -gF message-style "fg=#{@thm_teal},bg=$EF_MESSAGE_BACKGROUND,align=centre"
set -gF message-command-style "fg=#{@thm_teal},bg=$EF_MESSAGE_BACKGROUND,align=centre"

# menu
%if "#{>=:#{version},3.4}"
  set -gF menu-selected-style "#{E:@everforest_menu_selected_style}"
%endif

# panes
set -wgF pane-active-border-style "#{E:@everforest_pane_active_border_style}"
set -wgF pane-border-style "#{E:@everforest_pane_border_style}"

%if "#{==:#{@everforest_pane_status_enabled},yes}"
  # "internal" variables are kept as @_ef_p_.*
  # and then unset at the end.
  set -gq @_ef_p_left "" # the content on the left, usually a separator
  set -gq @_ef_p_middle ""
  set -gq @_ef_p_right ""
  set -gq @_ef_p_number ""
  set -gq @_ef_p_text ""

  %if "#{==:#{@everforest_pane_default_fill},none}"
    set -g @_ef_p_left \
      "#[fg=#{@thm_surface_0},bg=default]#{@everforest_pane_left_separator}"
    set -g @_ef_p_middle \
      "#[fg=#{@thm_fg},bg=#{@thm_surface_0}]#{@everforest_pane_middle_separator}"
    set -g @_ef_p_right \
      "#[fg=#{@thm_surface_0},bg=default]#{@everforest_pane_right_separator}"

    set -g @_ef_p_number \
      "#[fg=#{@thm_fg},bg=#{@thm_surface_0}]##{pane_index}"
    set -g @_ef_p_text \
      "#[fg=#{@thm_fg},bg=#{@thm_surface_0}]#{E:@everforest_pane_default_text}"

  %elif "#{==:#{@everforest_pane_default_fill},all}"

    set -g @_ef_p_left \
      "#[fg=#{E:@everforest_pane_color},bg=default]#{@everforest_pane_left_separator}"
    set -g @_ef_p_middle \
      "#[fg=#{E:@everforest_pane_color},bg=#{E:@everforest_pane_background_color}]#{@everforest_pane_middle_separator}"
    set -g @_ef_p_right \
      "#[fg=#{E:@everforest_pane_color},bg=default]#{@everforest_pane_right_separator}"

    set -g @_ef_p_number \
      "#[fg=#{E:@everforest_pane_background_color},bg=#{E:@everforest_pane_color}]##{pane_index}"
    set -g @_ef_p_text \
      "#[fg=#{E:@everforest_pane_background_color},bg=#{E:@everforest_pane_color}]#{E:@everforest_pane_default_text}"

  %elif "#{==:#{@everforest_pane_default_fill},number}"

    %if "#{==:#{@everforest_pane_number_position},left}"
      set -g @_ef_p_left \
        "#[fg=#{E:@everforest_pane_color},bg=default]#{@everforest_pane_left_separator}"
      set -g @_ef_p_right \
        "#[fg=#{E:@everforest_pane_background_color},bg=default]#{@everforest_pane_right_separator}"
    %else
      set -g @_ef_p_left \
        "#[fg=#{E:@everforest_pane_background_color},bg=default]#{@everforest_pane_left_separator}"
      set -g @_ef_p_right \
        "#[fg=#{E:@everforest_pane_color},bg=default]#{@everforest_pane_right_separator}"
    %endif

    set -g @_ef_p_middle \
      "#[fg=#{E:@everforest_pane_color},bg=#{E:@everforest_pane_background_color}]#{@everforest_pane_middle_separator}"

    set -g @_ef_p_number \
      "#[fg=#{E:@everforest_pane_background_color},bg=#{E:@everforest_pane_color}]##{pane_index}"
    set -g @_ef_p_text \
      "#[fg=#{E:@everforest_pane_color},bg=#{E:@everforest_pane_background_color}]#{E:@everforest_pane_default_text}"

  %endif

  %if "#{==:#{@everforest_pane_number_position},left}"
    set -wgF pane-border-format \
      "#{E:@_ef_p_left}#{E:@_ef_p_number}#{E:@_ef_p_middle} #{E:@_ef_p_text}#{E:@_ef_p_right}"
  %else
    set -wgF pane-border-format \
      "#{E:@_ef_p_left}#{E:@_ef_p_text} #{E:@_ef_p_middle}#{E:@_ef_p_number}#{E:@_ef_p_right}"
  %endif

  set -ug @_ef_p_left
  set -ug @_ef_p_middle
  set -ug @_ef_p_right
  set -ug @_ef_p_number
  set -ug @_ef_p_text
%endif

# popups
%if "#{>=:#{version},3.4}"
  set -gF popup-style "bg=#{@thm_bg},fg=#{@thm_fg}"
  set -gF popup-border-style "fg=#{@thm_surface_1}"
%endif

%if "#{==:#{@everforest_window_status_style},basic}"

  set -gq @everforest_window_left_separator " "
  set -gq @everforest_window_middle_separator " "
  set -gq @everforest_window_right_separator " "

%elif "#{==:#{@everforest_window_status_style},rounded}"

  set -gq @everforest_window_left_separator "#[fg=#{@_ef_status_bg},reverse]#[none]"
  set -gq @everforest_window_middle_separator " "
  set -gq @everforest_window_right_separator "#[fg=#{@_ef_status_bg},reverse]#[none]"

%elif "#{==:#{@everforest_window_status_style},slanted}"

  set -gq @everforest_window_left_separator "#[fg=#{@_ef_status_bg},reverse]#[none]"

  %if "#{==:#{@everforest_window_number_position},left}"
    set -gq @everforest_window_middle_separator "#[fg=#{@everforest_window_number_color},bg=#{@everforest_window_text_color}]"
    set -gq @everforest_window_current_middle_separator \
      "#[fg=#{@everforest_window_current_number_color},bg=#{@everforest_window_current_text_color}]"
  %else
    set -gq @everforest_window_middle_separator " #[fg=#{@everforest_window_number_color},bg=#{@everforest_window_text_color}]"
    set -gq @everforest_window_current_middle_separator \
      " #[fg=#{@everforest_window_current_number_color},bg=#{@everforest_window_current_text_color}]"
  %endif

  set -gq @everforest_window_right_separator "#[fg=#{@_ef_status_bg},reverse]█#[none]"

%endif

# DO NOT USE -o IN YOUR OWN CONFIGURATION
set -ogqF @everforest_window_current_left_separator "#{@everforest_window_left_separator}"
set -ogqF @everforest_window_current_middle_separator "#{@everforest_window_middle_separator}"
set -ogqF @everforest_window_current_right_separator "#{@everforest_window_right_separator}"

# window status
%if "#{!=:#{@everforest_window_status_style},none}"
  set -gF window-status-activity-style "bg=#{@thm_lavender},fg=#{@thm_crust}"
  set -gF window-status-bell-style "bg=#{@thm_yellow},fg=#{@thm_crust}"

   %if "#{==:#{@everforest_window_flags},icon}"
    set -gqF @_ef_w_flags "#{E:@everforest_window_flags_icon_format}"
  %elif "#{==:#{@everforest_window_flags},text}"
    set -gq @_ef_w_flags "#F"
  %else
    set -gq @_ef_w_flags ""
  %endif

  set -g @_ef_w_number_style "#[fg=#{@thm_crust},bg=#{@everforest_window_number_color}]"
  set -g @_ef_w_text_style "#[fg=#{@thm_fg},bg=#{@everforest_window_text_color}]"
  %if "#{==:#{@everforest_window_number_position},left}"
    set -gF window-status-format \
      "#{E:@_ef_w_number_style}#{E:@everforest_window_left_separator}#{@everforest_window_number}"
    set -agF window-status-format "#{E:@everforest_window_middle_separator}"
    set -agF window-status-format \
      "#{E:@_ef_w_text_style}#{@everforest_window_text}#{@_ef_w_flags}#{E:@everforest_window_right_separator}"
  %else
    set -gF window-status-format \
      "#{E:@_ef_w_text_style}#{E:@everforest_window_left_separator}#{E:@_ef_w_text_style}#{@everforest_window_text}#{@_ef_w_flags}"
    set -agF window-status-format "#{E:@everforest_window_middle_separator}"
    set -agF window-status-format \
      "#{E:@_ef_w_number_style} #{@everforest_window_number}#{E:@everforest_window_right_separator}"
  %endif

  # =======================================
  # And do the same for the current window.
  # =======================================

  set -g @_ef_w_number_style "#[fg=#{@thm_crust},bg=#{@everforest_window_current_number_color}]"
  set -g @_ef_w_text_style "#[fg=#{@thm_fg},bg=#{@everforest_window_current_text_color}]"
  %if "#{==:#{@everforest_window_number_position},left}"
    set -gF window-status-current-format \
      "#{E:@_ef_w_number_style}#{E:@everforest_window_current_left_separator}#{@everforest_window_current_number}"
    set -agF window-status-current-format "#{E:@everforest_window_current_middle_separator}"
    set -agF window-status-current-format \
      "#{E:@_ef_w_text_style}#{@everforest_window_current_text}#{@_ef_w_flags}#{E:@everforest_window_current_right_separator}"
  %else
    set -gF window-status-current-format \
      "#{E:@_ef_w_text_style}#{E:@everforest_window_current_left_separator}#{E:@_ef_w_text_style}#{@everforest_window_current_text}#{@_ef_w_flags}"
    set -agF window-status-current-format "#{E:@everforest_window_current_middle_separator}"
    set -agF window-status-current-format \
      "#{E:@_ef_w_number_style} #{@everforest_window_current_number}#{E:@everforest_window_current_right_separator}"
  %endif


  # Cleanup (unset) all of the internal variables.
  set -ug @_ef_w_number_style
  set -ug @_ef_w_text_style
  set -ug @_ef_w_flags
%endif

# Mode style. This is used for copy mode highlighting to style the current selection.
set -gF mode-style "bg=#{@thm_surface_0},bold"
set -gF clock-mode-colour "#{@thm_blue}"
